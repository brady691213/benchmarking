namespace Benchmarking.Linq.Tests;

public class ParquetReaderServiceTests
{
    [SetUp]
    public void Setup()
    {
        // Method intentionally left empty until I have some benchmarks running.
    }

    [Test]
    public void Test1()
    {
        // Test intentionally passes until I have some benchmarks running.
        Assert.Pass();
    }
}